# SimpleMindMap MCP Server

一个MCP服务器，支持让AI生成思维导图。

# 使用

修改你使用的AI客户端的MCP配置文件：

```json
{
  "mcpServers": {
    "excalidraw": {
      "command": "npx",
      "args": ["-y", "simple-mind-map-mcp"]
    }
  }
}
```

在你使用的AI客户端中，添加skill技能：[技能地址](./sikll/)

# 安装

# 本地开发
